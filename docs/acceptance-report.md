# Development acceptance report

This report records evidence available in the local development environment. It is intentionally not a production-release sign-off: Linux/production FRP deployment matrix, Cloudflare Sandbox, ACME Staging, production TLS termination, formal security scanners and signing still require their external environments.

## Current revision evidence

The latest verified implementation revision is `e07cd8f2fc6b29038664aaa14adbeaae7a156123`. Its PR CI run
[`31467891623`](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31467891623)
and CodeQL run
[`93704705991`](https://github.com/sshiong/frp-panel-platform-v3/runs/93704705991)
passed all required checks, including contract, coverage, Go race/static analysis,
both panel builds and WCAG checks, fixed Linux FRP E2E, fault injection, fuzz,
security, container scanning, release metadata and the fixed Ubuntu 24.04 / 2 vCPU /
2 GiB target acceptance profile. The measured target profile records PERF-001/002
read/write p95 55.457712/37.158087ms, PERF-003 112.743114ms, PERF-005 4.404769ms, PERF-006
7.064798ms and PERF-007 WebSocket/HTTP/old-FRP invalidation 61.597603/0.305122/0.381659ms.
The hosted target artifact also passed the fixed FRP TCP, FRPC `verify`, and
FRPS/FRPC Plugin network E2E steps. The current local collector report is regenerated
against the exact report checkout and records 7 passed, 0 failed and 4 blocked steps;
the three additional blocked steps are the intentionally unconfigured local fixed-FRP
runtime, while the reviewed Provider, target-environment, signing and owner-approval
bundle remains absent. The 142-record acceptance
index is also bound to the exact report checkout and remains `blocked` while the matrix has
incomplete external rows. This revision also adds Cloudflare Token clear/rotation
guards across DNS and ACME Jobs, Token verification, and explicit activation; the
running-job regression proves stale DNS/ACME results are blocked before local success
publication and a cleared candidate cannot become active. PR #2 remains open for
required human review.

Target-environment automation is now available through `make target-acceptance` and
the manual [`target-acceptance.yml`](.github/workflows/target-acceptance.yml). It
binds the fixed Ubuntu 24.04 / 2 vCPU / 2 GiB / SQLite WAL profile to the shared
performance script, Linux fault injection, fixed FRP v0.68.0 verification, and
real TCP/Plugin E2E. The collector writes a schema-checked redacted report and
mode `0600` logs, returning exit code `2` when the host or fixed artifacts are not
available. This automation is evidence tooling; it does not claim production
hardware, Provider/ACME, signing, or owner approval.

## Verified locally

- Separate Server and Client Go modules compile and test independently.
- `make build` and the Docker build stages embed the matching Admin/Client static assets into the corresponding Go binary; external `FRP_*_WEB_DIR` paths are explicit overrides only.
- Two independent Vue 3 + TypeScript + Vite + Element Plus apps build successfully.
- Admin login does not render a Server address field; Client login does not render admin or device-registration fields.
- Client Panel exposes separate Mapping and Domain Binding surfaces; the Domain Binding view shows normalized IDNA names and keeps `pending_dns` visibly distinct from `active`.
- Frontend policy tests cover the Admin first-login credential gate/destructive-action re-auth boundary and Client status/dangerous-operation guards at 100% line/branch/function coverage for the policy modules.
- Argon2id password hashing, first-login password change and user creation work.
- Second Client login returns `SESSION_REPLACED` for the old opaque session.
- TCP Mapping auto-allocation reserves port `6000`; TCP/UDP conflict returns upstream HTTP 409.
- Desired/applied config versions converge after Client verifies the signed snapshot and applies through Supervisor.
- HTTP Mapping and IDNA/Punycode Domain Binding creation produce explicit `pending_dns` states.
- SQLite WAL encrypted backup endpoint generated an `.fppb` package in a temporary test data directory.
- FPPB1 decode, password rejection, SQLite integrity validation, and atomic restore with a recoverable pre-restore filename pass unit tests.
- Cloudflare HTTP Provider unit tests cover bearer auth, zone-record discovery, create, update, and delete without requiring a live network.
- Cloudflare Token replacement now has a tested `pending → verified_pending → valid` handoff: paginated capability verification records accessible Zones and impacted domains, reauthentication is required at activation, failed/unconfirmed replacement preserves the active credential, and the Client Panel local proxy never caches the Token. Clear retires the lifecycle row while wiping ciphertext/Nonce/cache and canceling queued token-verification jobs; the Client UI enforces the required 3-second confirmation countdown.
- Cloudflare clear/rotation race coverage now binds DNS, managed-record deletion and ACME Jobs to the decrypted `token_version`: every Cloudflare HTTP request, ACME DNS-01 checkpoint and local success commit rechecks the active credential. A clear during external work returns durable `BlockedError`; the regression proves no stale managed DNS record or certificate file/status is published. Real Cloudflare/ACME Sandbox evidence remains external.
- Provider-level guard tests additionally cover the compound Cloudflare `UpsertDNS` GET/POST boundary and the real ACME DNS-01 provider before order creation; both stop before the next network request when the credential guard fails.
- Token verification and explicit activation now use version/status-specific request guards through Zone impact discovery and the final activation transaction; clear/rotation during either phase cannot continue stale Provider work or mark the cleared version active. ACME account registration also checks the guard immediately before its first external request.
- Domain creation persists and validates A/AAAA/CNAME intent, TTL and HTTPS-derived `proxied` state before the DNS job; the Client Domain surface displays the effective record values.
- Durable Job Worker tests cover deduplication, lease heartbeat, expired-lease reclaim, retry backoff, Cloudflare token verification, DNS conflict handling and Router snapshot promotion.
- Mapping updates enforce `expected_revision` and persist PUT idempotency records; long-running jobs renew their leases, Cloudflare 401/403 errors are classified as permission failures, and ACME renewal/deletion jobs are re-seeded after restart.
- Domain deletion removes the Router route after the Client acknowledgement and only deletes Cloudflare DNS when `managed_by_panel=true`; adopted unmanaged records remain external.
- Router tests cover HMAC/hash rejection, last-good retention, control/business Host routing, unknown Host 404 and offline upstream 502. Router runtime has no database dependency.
- ACME DNS-01 provider is implemented with the Go ACME client, encrypted account material, TXT propagation polling and cleanup; it remains disabled unless an operator supplies a staging/production directory, email and verified Cloudflare Token.
- FRPS fixed-binary process tests cover SHA-256 verification, PID ownership, proxy-only reload in place, last-good rollback restart, orphan recovery and controlled stop.
- An opt-in test starts fixed official FRP v0.68.0 FRPS and FRPC with a real loopback Server Plugin, creates a real Server Session/Mapping metadata set, and proves Login/NewProxy/WorkConn/NewUserConn plus the proxied TCP payload; the ordinary protocol test covers Ping/CloseProxy rejection paths.
- Server/Client WebSocket tests cover the v1 envelope boundary, heartbeat session renewal, remote disable cleanup, bounded exponential backoff with jitter, and signed full-sync recovery after configuration notifications.
- Pending quotas cover mappings, pending port leases, domain operations and certificate jobs; metrics expose resource counts, job backlog, Router lag and SQLite WAL size without user-input labels.
- Mapping deletion retains domain bindings until managed DNS cleanup succeeds; a focused service test proves managed records are deleted and mapping operations only finalize after compensation. Port rotation proves the old lease is released only after the new revision applies.
- Revision failure rollback is transactional: the pending revision is retained as `failed`, `pending_revision_id` is cleared, the previous active revision and port remain authoritative, and a replacement port lease is released. A follow-up update can create the next immutable revision.
- Administrator user deletion enters a durable `deleting` state, revokes sessions/runtime credentials immediately, shuts down the Client path, cleans Domain/Mapping resources in dependency order, retains the user operation after local row removal, and records unresolved external DNS residue when forced cleanup is requested. The admin Operation API and UI expose each residue's provider, identifier and reason, with a separate audit event.
- Client startup writes an ownership PID marker for fixed-binary FRPC and recovers an orphan only after matching the configured binary command line; recovery removes runtime configuration/secrets and refuses PID-reuse termination.
- Server admin cookie writes require a session-bound CSRF token; Cloudflare token changes, FRP credential rotation, user lifecycle changes, Router rebuild and encrypted backup require a short-lived reauthentication ticket. Client LAN exposure requires an explicit non-loopback bind, CIDR/Host allowlist and TLS certificate/key, and service-to-service HTTP clients reject redirects.
- Versioned master/certificate key rings retain old keys during migration. The `make key-rotate` maintenance path re-wraps FRP credentials, Cloudflare Tokens and certificate private keys in a SQLite transaction; crypto restart compatibility and service row-count/re-login tests pass. A real production rotation window remains unsigned-off external work.
- Before Client login, the Server certificate is inspected without sending the password. HTTPS connections require normal certificate validation or an explicit operator-confirmed SPKI pin; the pin is memory-only, bound to the selected Server, and a wrong pin fails before an HTTP handler is reached. IP endpoints therefore cannot silently bypass certificate trust.
- Client Server switching logs out the old Server, stops the old FRPC path, clears runtime secrets and cache, and starts one new Client Session. Offline fallback is limited to GET snapshots for the current session; session replacement/disable and all 4xx responses invalidate or surface the error instead of serving stale cache. Browser `localStorage` contains only `last_server_panel_url`; CSRF and runtime secrets remain in memory.
- Mapping toggle, Domain deletion and DNS conflict actions persist request hashes under the authenticated session generation; retries return the original outcome and conflicting bodies return `IDEMPOTENCY_KEY_REUSED`. Toggle also enforces optional expected config/revision values.
- All authenticated Server write routes require `Idempotency-Key`. Resource transactions retain their typed idempotency records and the remaining write routes use a shared encrypted-response replay layer; same-key/different-body conflicts return 409, while replayed responses receive the current request ID. Unsupported non-empty protocol versions return RFC-style `426 UPGRADE_REQUIRED`.
- Client/Server version compatibility is enforced before Client Session creation: `X-FRP-Client-Version` is validated, old/invalid Clients receive `426 CLIENT_VERSION_UNSUPPORTED` plus `Upgrade-Required` metadata, and supported but older Clients receive a visible upgrade suggestion without a parsing failure.
- Independent release metadata is now build-enforced: `make build SERVER_VERSION=x.y.z CLIENT_VERSION=a.b.c` injects independent versions into the Server/Client binaries and compatibility API; release manifests also carry the minimum/latest Client and minimum FRPC versions, with SemVer policy validation before manifest generation.
- `http_only` is enforced as a fixed no-certificate/no-redirect mode in the API, service layer, Client form and SQLite CHECK/trigger migration, preventing contradictory TLS configuration from reaching a DNS or Router job.
- Client Operations exposes phase, step, status, error, retry and external-residue information, giving long-running asynchronous work an explicit loading/error/retry surface.
- Formal FPPB1 backup tests include protected data-directory files, per-entry checksums, wrong-password rejection, SQLite integrity validation, session/runtime credential revocation and atomic restore.
- `scripts/validate-openapi.rb` validates both OpenAPI 3.1 contracts, unique operation IDs, WebSocket metadata and independent implementation route parity: Server 34 paths / 39 operations and Client Local API 20 paths / 23 operations. `make sbom` emits a local SPDX inventory, `make checksums` emits SHA-256 checksums for panel and fixed FRP artifacts, and the release manifest records FRP v0.68.0 hashes. Artifact signing and third-party scanner attestations remain release work.
- The implementation-level response contract test now checks the actual authenticated `/me` session fields and request metadata; every successful JSON response in both OpenAPI contracts must declare a response schema. Previous-stable migration rehearsal, Vue ESLint gate, npm license allowlist, independent panel version metadata, and Router header/body/upstream timeout boundaries are run by `make contract`, `make migration-check`, `make lint`, `make license`, and CI.
- `openapi-typescript` generates `contracts/generated/server-api.d.ts` and `contracts/generated/client-api.d.ts`; both panel request layers are typed against their corresponding generated OpenAPI paths, both model layers consume generated schema types, and CI fails when regeneration changes either committed contract artifact.
- Coverage gate: `make coverage` now measures all `internal/...` Go packages and enforces at least 75% per module plus at least 90% for Server authentication, purpose-specific encryption, and Router runtime/snapshot packages. The current local measurement is Server 77.10%, Client 76.62%, with the critical packages at 90.48%/92.64%/92.45%; the command is also a required GitHub `coverage` job.
- Final local regression: Server and Client `go test -race ./...`, `make lint`, `make build`, `make contract`, `make security`, 2-second fuzz targets, the PERF-001~007 local profile, fixed FRPC/FRPS v0.68.0 `verify`, checksum validation, native FRPS/FRPC TCP network E2E and the opt-in real FRPS/FRPC + Plugin metadata E2E all pass. The fixed Ubuntu 24.04 Docker 2 vCPU/2 GiB profile now also passes PERF-001/002/003/005/006/007; production deployment capacity, disk and network behavior remain separate external evidence. The release build emits only `frp-panel-server` and `frp-panel-client`; the backup restore command remains a Server source-level operational tool. The two panel builds now use only the required Element Plus component styles and emit 225.44/235.93 kB JavaScript and 66.13/69.74 kB CSS without a Vite chunk warning. Docker image build/scan was not locally executable because the current Docker daemon was unavailable, so it remains an external CI/release gate.
- Final hosted CI evidence: commit [`2f73156`](https://github.com/sshiong/frp-panel-platform-v3/commit/2f731567da6933d4fc2ae1db333ad9d61fc2ca19) passed all nine [`ci` jobs](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/30745496136), including security, dual-panel lint/build, Go race/static analysis, fuzz, contract/migration/license, container scan and release metadata; the matching [`CodeQL run`](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/30745496145) also passed.
- Latest hosted PR evidence: commit [`4e0cbc1`](https://github.com/sshiong/frp-panel-platform-v3/commit/4e0cbc1) passed all required [`ci` contexts](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/30760001938), including the Docker container scan and release metadata, and the matching [`CodeQL run`](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/30760001941) passed for Go and TypeScript.
- Final hosted PR evidence: commit [`0f0b353`](https://github.com/sshiong/frp-panel-platform-v3/commit/0f0b3538ba678ec685deb72e283dce9d4df9b038) passed all required [`ci` contexts](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/30761382419), including security, accessibility, fuzz, release metadata and container scan; the matching [`CodeQL run`](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/30761382415) passed for Go and TypeScript. PR #2 remains open pending required human review.
- Browser visual QA: Playwright checked the independent Admin and Client login pages with the graphite/ivory/steel-blue/state-color system; no pale-purple theme is present. Existing authenticated visual evidence remains [`admin-overview.png`](../output/playwright/admin-overview.png), [`client-tunnels.png`](../output/playwright/client-tunnels.png), [`client-domains.png`](../output/playwright/client-domains.png).
- Responsive QA: Playwright checked both login pages at 390×844; Admin and Client report `scrollWidth === innerWidth === 390`, mobile panels stack, tables keep intentional local overflow, important controls use 44px minimum touch targets, and reduced-motion overrides are present. The scored technical review is recorded in [`ui-audit.md`](ui-audit.md).
- Latest navigation semantics regression: Admin and Client sidebar buttons now expose `aria-current="page"` for the active surface and explicit `type="button"`; the authenticated Playwright audit verifies exactly one current navigation item after every page transition. The local `make accessibility`, `make lint`, and `make contract` gates passed.
- Latest hosted verification for implementation revision [`75fb6e6`](https://github.com/sshiong/frp-panel-platform-v3/commit/75fb6e6f5fb583beda4dffb9c68c319cc3d0127c): [`ci run 31365422945`](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31365422945) and [`CodeQL run 31365422966`](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31365422966) passed, including coverage, Go race/static analysis, dual-panel build/WCAG, fixed Linux FRP E2E, fault injection, fuzz, security, container scanning and release metadata. The fixed-artifact local collector records 9 passed, 0 failed and 1 blocked; the only blocked step remains reviewed Provider/ACME/target-environment/signing/owner evidence, and PR #2 remains open for required human review.
- Latest hardening verification for implementation revision [`eaed6bf`](https://github.com/sshiong/frp-panel-platform-v3/commit/eaed6bf230ddd887252d9a4fbf9e475a49972ba4): every Admin/Client button declares an explicit `type`, and the accessibility runner fails if a future button omits it. [`ci run 31366537532`](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31366537532) and [`CodeQL run 31366537522`](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31366537522) passed, including dual-panel WCAG, Go race/static analysis, fixed Linux FRP E2E, fault injection, security, container scanning and release metadata. The local fixed-artifact collector remains 9 passed, 0 failed and 1 blocked; no external Provider or release sign-off is inferred.
- Latest verified implementation revision [`b444975`](https://github.com/sshiong/frp-panel-platform-v3/commit/b444975d7dc6b4ab1935bedf9d9229ac1dfe1c49): [`ci run 31368552030`](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31368552030) and [`CodeQL run 31368552064`](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31368552064) passed all required jobs. The local collector records 9 passed, 0 failed and 1 blocked; each collector step now includes the standard environment/steps/expected/actual/artifacts/operator/time fields and a redacted 0600 log artifact. The blocked state still represents missing reviewed Provider/ACME/target-environment/signing/owner evidence, not a release pass.
- Release workflow gate hardening: `.github/workflows/release.yml` now installs Staticcheck and Chromium, reruns `make test lint accessibility`, and executes the shared fixed 2 vCPU/2 GiB performance profile on the exact release checkout before the fixed-FRP/external-evidence gate and before cosign signing. `scripts/release-workflow-policy.rb` enforces that ordering; this closes the gap where a release could otherwise rely only on historical PR checks or stale performance evidence.
- Fixed performance profile implementation: `.github/workflows/performance.yml` defines an Ubuntu 24.04 Docker profile constrained to exactly 2 vCPU and 2 GiB RAM, runs the complete PERF-001/002/003/005/006/007 suite with SQLite WAL, uploads a separate log, and is protected by `scripts/performance-workflow-policy.rb`; the result is recorded below.
- Fixed performance profile result: the target-acceptance artifact from [`ci run 31467891623`](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31467891623) passed the Ubuntu 24.04 Docker profile constrained to 2 vCPU/2 GiB. The uploaded measured log records PERF-001/002 p95 55.457712ms/37.158087ms with zero errors, PERF-003 112.743114ms, PERF-005 4.404769ms, PERF-006 7.064798ms, and PERF-007 WebSocket/HTTP/old-FRP invalidation 61.597603ms/0.305122ms/0.381659ms. These satisfy the documented reference baseline; production deployment capacity and network sign-off remain separate release evidence.
- Fixed-profile runner repair record: the first attempt [`performance run 31370268438`](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31370268438) failed before tests because `bash -lc` removed the Go image PATH; it produced no performance result. The workflow now uses non-login `bash -c`, the policy locks that boundary, and only the subsequent successful run is used as acceptance evidence.
- Current release evidence binding: revision [`639a184`](https://github.com/sshiong/frp-panel-platform-v3/commit/639a18483054072b9e273a09323fbaf872b1c0f2) passed [`ci run 31371628773`](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31371628773) and [`CodeQL run 31371628772`](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31371628772). The release checkout invokes the shared fixed 2 vCPU/2 GiB performance script before external evidence and cosign signing; the revision-bound collector recorded 9 passed, 0 failed and 1 blocked, so absent reviewed Provider/target/signing/owner evidence remains blocked.
- Accessibility automation: the built Admin and Client panels pass axe WCAG 2.1 AA, unlabeled-control checks, keyboard focus/reduced-motion checks, and 390px horizontal-overflow checks through `npm run test:accessibility`. The deterministic fixture also scans every authenticated navigation surface and the Admin create-user, Client Mapping, and Client Domain dialogs; the root Playwright tool is wired into the existing `web (admin)` CI matrix job.
- External evidence collection: `make external-acceptance` runs the complete `make contract` target, generates the 141-item acceptance evidence index, then runs migration/security/license/build/local-performance gates and fixed FRPS/FRPC checks when explicitly configured. It writes a redacted machine-readable report. A reviewed evidence bundle is schema-checked per gate for environment, steps, expected/actual results, artifacts, operator, and execution time; `SEC-008` must include cosign identity/issuer metadata and `DOD-001` must include three distinct same-revision release/security/test approvals. Missing Linux, Cloudflare Sandbox, ACME Staging, target-hardware, fault-injection or signing evidence is recorded as `blocked` with exit code `2`, never as a pass. The procedure is documented in [`external-acceptance.md`](external-acceptance.md).
- Evidence integrity is now tied to the release revision: `scripts/test-external-acceptance.rb` rejects evidence from another repository or commit, and `scripts/acceptance-matrix-policy.rb` ensures all 141 standard acceptance IDs remain represented by the tracked matrix (with only the derived DOD row allowed). Both checks run through `make contract` and CI.
- Latest fixed-artifact collector run: the isolated FRP v0.68.0 fixture passed local contract/migration/security/license/build, native FRPS/FRPC TCP networking, fixed FRPC `verify`, and the real FRPS/FRPC Plugin network E2E. The collector correctly returned `blocked` with exit code `2` only for the missing reviewed Cloudflare/ACME/target-hardware/fault-injection/signing evidence bundle.
- Latest hosted verification for that evidence update: commit [`49a21b0`](https://github.com/sshiong/frp-panel-platform-v3/commit/49a21b00f0e75afbf1c30772ab210e8b9d3dc98c) passed [`ci` run 30762128928](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/30762128928) and [`CodeQL` run 30762128925](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/30762128925), including the container scan, release metadata, security, accessibility, fuzz and dual-panel checks. PR #2 remains open pending required human review.
- Latest hosted verification for the authenticated-panel and evidence-schema update: commit [`92cf1a3`](https://github.com/sshiong/frp-panel-platform-v3/commit/92cf1a379aab096dc2800a92eaa3d43e41a5e77c) passed [`ci` run 30763715852](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/30763715852) and [`CodeQL` run 30763715867](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/30763715867), including the new contract evidence-schema test, authenticated Admin/Client accessibility scan, security, fuzz, container scan, and release metadata. PR #2 remains open pending required human review.
- Latest hosted verification for the frontend performance/token closure: commit [`b91412f`](https://github.com/sshiong/frp-panel-platform-v3/commit/b91412f00d73d70f54bbdae8eade65339f98f4be) passed [`ci` run 30764876448](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/30764876448) and [`CodeQL` run 30764876452](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/30764876452), including the CSS token policy, dual-panel typecheck/lint/build/accessibility, security, fuzz, container scan, release metadata, and Go static analysis. PR #2 remains open pending required human review.
- Latest hosted verification for the independent Server/Client release-version closure: commit [`a1cb4ae`](https://github.com/sshiong/frp-panel-platform-v3/commit/a1cb4ae) passed [`ci` run 30766492825](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/30766492825) and [`CodeQL` run 30766492828](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/30766492828), including independent version policy, release metadata, security, accessibility, fuzz, container scan and dual-panel checks. PR #2 remains open pending required human review.
- Latest hosted verification for the acceptance-evidence integrity closure: commit [`97f5d9e`](https://github.com/sshiong/frp-panel-platform-v3/commit/97f5d9e) passed [`ci` run 31180964158](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31180964158) and [`CodeQL` run 31180966657](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31180966657), including the standard-to-matrix policy, current-commit evidence binding, security, accessibility, fuzz, container scan and release metadata. PR #2 remains open pending required human review.
- Latest hosted verification for the final acceptance-evidence documentation: commit [`2a058fe`](https://github.com/sshiong/frp-panel-platform-v3/commit/2a058fe3b07ab9505ebeae3d4fb77e3b5abd4a55) passed [`ci` run 31181350965](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31181350965) and [`CodeQL` run 31181352584](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31181352584), including the standard-to-matrix policy, current-commit evidence binding, security, accessibility, fuzz, container scan and release metadata. PR #2 remains open pending required human review.
- Latest local external-acceptance rehearsal with Go 1.25.4 and fixed FRP v0.68.0 artifacts passed local contract, migration, security, license, build, native TCP E2E, fixed FRPC `verify`, and the real FRPS/FRPC Plugin network E2E. The report is bound to the current revision and returns `blocked` with exit code `2` only for the missing Cloudflare/ACME/target-hardware/fault-injection/signing evidence bundle; no external pass is inferred.
- Latest hosted verification for the fixed FRP acceptance rehearsal: commit [`9fe4b92`](https://github.com/sshiong/frp-panel-platform-v3/commit/9fe4b92) passed [`ci` run 31183385079](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31183385079) and [`CodeQL` run 31183385781](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31183385781), including the contract/migration/security checks, Go race/static analysis, dual-panel accessibility, fuzz, container scan, and release metadata. PR #2 remains open pending required human review.
- Repository governance evidence: the public repository has `main` protection with linear history, force-push/deletion disabled, code-owner review, two required approvals, and all required CI/CodeQL contexts; `frp-linux-e2e`, `fault-injection`, and `CodeQL` are explicitly required merge checks. The current CODEOWNERS file identifies the repository owner and explicitly leaves the second security/database/crypto owner as a release prerequisite.
- Release workflow hardening: the release job now requires the root `release-evidence.json` bundle for the exact revision, runs fixed FRP v0.68.0 native TCP and Plugin E2E before signing, uses the `production-release` environment, and verifies every keyless cosign signature against the GitHub Actions OIDC issuer and the exact release workflow/ref after signing. `scripts/release-workflow-policy.rb` and the contract CI job prevent regressions in this order. Missing real Provider/ACME/target-environment/fault-injection/sign-off evidence fails closed before artifact publication.
- Dependency security regression fix: the Admin and Client lockfiles now resolve `nanoid` 3.3.18 instead of vulnerable 3.3.16; clean installs and npmjs registry high-severity audits both report zero vulnerabilities locally. Hosted security CI must reconfirm the current revision.
- Latest hosted verification for the dependency security and release-signature policy update: commit [`b18ffc3`](https://github.com/sshiong/frp-panel-platform-v3/commit/b18ffc3) passed [`ci` run 31268337419](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31268337419) and [`CodeQL` run 31268337422](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31268337422); security, dual-panel accessibility/build, Go race/static analysis, fuzz, fixed FRP E2E, fault injection, container scan and release metadata all passed. The revision-bound external collector remains `blocked` only for absent reviewed Provider/ACME/target-environment/signing/owner-signoff evidence.
- Latest hosted verification after a fuzz-discovered security fix: commit [`935a1f7`](https://github.com/sshiong/frp-panel-platform-v3/commit/935a1f7) rejects malformed host characters such as `%` during Client server-address normalization and adds a regression test. The matching [`ci` run 31185032520](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31185032520) and [`CodeQL` run 31185032236](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31185032236) passed, including all fuzz targets, security, accessibility, container scan and release metadata. PR #2 remains open pending required human review.

- Linux acceptance automation: the CI workflow now has an Ubuntu 24.04 `frp-linux-e2e` job that verifies the official FRP v0.68.0 release digest, runs fixed `frps/frpc verify`, native TCP networking, and real FRPS/FRPC + loopback Plugin E2E. The manual performance workflow also runs the target-size profile on Ubuntu 24.04 and uploads its log. These are stronger hosted gates, but they do not claim target-production hardware, Cloudflare/ACME, fault-injection, signing, or owner sign-off.
- Linux fault-injection automation: `make fault-injection` now mounts a disposable 32MiB tmpfs, fills it to `ENOSPC`, and proves Router atomic snapshot failure preserves `last-good`; the same Ubuntu 24.04 CI job runs Provider/ACME HTTP `Date` skew fail-safe tests. Backup/restore disk pressure and system-clock/target-environment release evidence remain external.
- Hosted fault-injection evidence: commit [`fad3038`](https://github.com/sshiong/frp-panel-platform-v3/commit/fad30389ab3a8e6d5c00e4636330ddc6e5c0bdfb) passed [`ci` run 31189601927](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31189601927), including the Ubuntu 24.04 disposable tmpfs `ENOSPC` job, and [`CodeQL run 31189601869`](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31189601869). This closes the repeatable implementation-level fault-boundary check without claiming target release backup/restore or system-clock sign-off.
- Backup failure safety hardening: `RestoreWithOptions` now rolls back the newly installed database to the `.before-restore-*` target if post-install session invalidation or protected-file restoration fails; the backup test suite also proves a full output filesystem returns an error without leaving a partial archive. Target-host disaster-recovery evidence remains external.
- Hosted target-scale performance evidence: [`performance run 31191465839`](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31191465839) passed on Ubuntu 24.04 and uploaded `linux-target-scale-performance`. Recorded values were PERF-001 read p95 102.321716ms, PERF-002 write p95 45.921474ms, PERF-003 Router snapshot 523.973835ms, PERF-005 config generate/sign 4.54487ms, PERF-006 Client apply 6.235335ms, and PERF-007 WebSocket/HTTP/FRP invalidation 65.514317ms/0.376666ms/0.325481ms. These are hosted-run evidence, not a fixed 2 vCPU/2 GiB production-capacity sign-off.
- WAL pressure recovery automation: `TestCheckpointUnderWALPressure` writes 2000 rows with autocheckpoint disabled, verifies that WAL bytes are produced, runs the explicit checkpoint, checks row durability, and reopens the database to verify restart recovery. The Ubuntu 24.04 disposable fault-injection job now runs this check beside Router `ENOSPC`, backup archive, and clock-skew checks; long-duration production-disk behavior remains external.
- Latest hosted verification for the WAL pressure/fault-injection update: commit [`92efff8`](https://github.com/sshiong/frp-panel-platform-v3/commit/92efff808529ba3a8a0462daf5b6c93bcd46428b) passed [`ci run 31192487455`](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31192487455) and [`CodeQL run 31192488274`](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31192488274). The CI run includes Server/Client race and static analysis, both panels' typecheck/lint/build and WCAG checks, fixed Linux FRP E2E, container scan, release metadata, and the new Ubuntu fault-injection job.
- Latest revision-bound local external collector: with the official FRP v0.68.0 macOS ARM binaries and the self-managed HTTP fixture, local contract, migration, security, license, build, native TCP E2E, fixed FRPC `verify`, and real FRPS/FRPC Plugin network E2E pass. The report correctly stays `blocked` with exit code `2` only for the absent reviewed Cloudflare/ACME/TLS/target-environment/fault-injection/signing/owner-signoff evidence; Ubuntu target deployment remains covered by the hosted Linux job, not this macOS run.
- Latest hosted verification for the self-managed FRP fixture lifecycle: commit [`8357f16`](https://github.com/sshiong/frp-panel-platform-v3/commit/8357f16) passed [`ci` run 31269140501](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31269140501) and [`CodeQL` run 31269140438](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31269140438). The Ubuntu 24.04 FRP job now exercises the same fixture-start/ready/cleanup path used by the local collector.
- External-provider runner preparation: commit [`82d9dd3`](https://github.com/sshiong/frp-panel-platform-v3/commit/82d9dd3) adds a guarded Cloudflare disposable-zone DNS lifecycle runner and a guarded ACME Staging DNS-01 runner. The runners redact secrets, refuse unconfirmed or production endpoints, clean temporary records/material, and are compiled by the contract gate. They make the real evidence procedure executable but do not create Cloudflare/ACME evidence without operator-supplied disposable credentials.
- Manual external workflow preparation: commit [`bc19657`](https://github.com/sshiong/frp-panel-platform-v3/commit/bc19657) adds a `workflow_dispatch`-only, `external-acceptance`-environment workflow with secret-bound Cloudflare/ACME steps, redacted 14-day artifacts, and a final two-runner gate; `scripts/external-workflow-policy.rb` prevents automatic push/PR activation or confirmation removal.

- Latest hosted Linux FRP acceptance: commit [`98c9495`](https://github.com/sshiong/frp-panel-platform-v3/commit/98c9495) made the Makefile shell and Go cache defaults portable across macOS/Linux. The matching [`ci` run 31187605736](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31187605736) passed the Ubuntu 24.04 fixed-FRP digest, verify, native TCP, and real Plugin E2E job, and [`CodeQL run 31187605700`](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31187605700) passed for Go and TypeScript. This still does not sign off target production hardware, Cloudflare/ACME, fault injection, signing, or owner approvals.
- Final acceptance-report refresh: commit [`49844b8`](https://github.com/sshiong/frp-panel-platform-v3/commit/49844b8335ab81c6c1beeae15832a1f85fe46141) passed [`ci` run 31188168754](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31188168754) and [`CodeQL run 31188169920`](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31188169920). The revision-bound external collector passed fixed FRP native TCP, FRPC verify, and real Plugin E2E, then returned `blocked` with exit code `2` because reviewed Provider/Cloudflare/ACME/target-hardware/fault-injection/signing/owner-signoff evidence is still absent.
- Current hosted verification: commit [`b56f750`](https://github.com/sshiong/frp-panel-platform-v3/commit/b56f7509d768ff7b3a2c26c8cbc2c184d4dfb4b2) passed [`ci` run 31314283976](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31314283976) and [`CodeQL` run 31314283974](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31314283974). The run includes the reproducible Go 1.25.4 contract/lint baseline, dual-panel WCAG checks, race/static analysis, fuzz, fixed Linux FRP E2E, fault injection, security, container scan and release metadata. The current local collector report is bound to this revision with 8 passed, 0 failed and 1 blocked step; no external Provider or release sign-off is inferred.
- Latest hosted verification: commit [`480d8d7`](https://github.com/sshiong/frp-panel-platform-v3/commit/480d8d7ba6388b0c0c679c806cb2cb3c7f4921cf) passed [`ci` run 31328380724](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31328380724) and [`CodeQL` run 31328380709](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31328380709). This revision also raises both panels' icon-button touch targets to 44×44px; the hosted run passed the dual-panel WCAG checks, Go/static analysis, FRP E2E, fault injection, fuzz, security, container scan and release metadata. The current local collector report is bound to this revision with 8 passed, 0 failed and 1 blocked step; external Provider, target-hardware, signing and owner-signoff evidence remains absent.
- Latest hosted verification: commit [`f7cdb4a`](https://github.com/sshiong/frp-panel-platform-v3/commit/f7cdb4a342edd821a7086b5beb885e36fa7881f2) passed [`ci` run 31329804179](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31329804179) and [`CodeQL` run 31329804182](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31329804182). Frontend API calls now use generated OpenAPI paths, methods, parameters, request bodies and responses, with a contract policy preventing untyped regressions; the hosted run passed all existing Go, web, FRP E2E, fault-injection, fuzz, security, container and release gates. The current local collector report is bound to this revision with 8 passed, 0 failed and 1 blocked step; external Provider, target-hardware, signing and owner-signoff evidence remains absent.
- Latest hosted verification: commit [`c3ea595`](https://github.com/sshiong/frp-panel-platform-v3/commit/c3ea5952416445fcb7d08d0caae68b367b9e7500) passed [`ci` run 31330429823](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31330429823) and [`CodeQL` run 31330429810](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31330429810). External acceptance report redaction now includes the actual Cloudflare E2E token boundary and has a regression test; all Go, web, contract, FRP E2E, fault-injection, fuzz, security, container and release gates passed. The revision-bound local collector records 8 passed, 0 failed and 1 blocked step, and its report secret scan is clean; real Provider, target-hardware, signing and owner-signoff evidence remains absent.
- Latest hosted verification: commit [`9654983`](https://github.com/sshiong/frp-panel-platform-v3/commit/9654983b7d2fabe6c67ded3a3914b11dabf9484f) passed [`ci` run 31331663432](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31331663432) and [`CodeQL` run 31331663449](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31331663449). The contract job now enforces OpenAPI client-call boundaries, operation metadata boundaries, and standard-to-matrix coverage; the full run also passed Go, dual-panel, fixed Linux FRP E2E, fault-injection, fuzz, security, container scan, release metadata, and CodeQL checks. The revision-bound local collector remains 8 passed, 0 failed and 1 blocked, with the report secret scan clean; real Provider, target-hardware, signing, and owner-signoff evidence remains absent.
- Latest hosted verification: commit [`527c7ea`](https://github.com/sshiong/frp-panel-platform-v3/commit/527c7ea4cf252625365aac4d00bf2e66d2e8bc16) passed [`ci` run 31332973081](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31332973081) and [`CodeQL` run 31332973030](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31332973030). The new Go coverage policy job passed and is now a required `main` status check; the full run also passed the crypto regression, Go, dual-panel, fixed Linux FRP E2E, fault-injection, fuzz, security, container scan, release metadata, and CodeQL gates. The revision-bound local collector remains 8 passed, 0 failed and 1 blocked; real Provider, target-hardware, signing, and owner-signoff evidence remains absent.
- Latest hosted verification: commit [`763c559`](https://github.com/sshiong/frp-panel-platform-v3/commit/763c559a86efb3de01585f432c274bbc5365d988) passed [`ci` run 31333890995](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31333890995) and [`CodeQL` run 31333891002](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31333891002). The run passed the strict ACME Staging URL regression, production service URL boundary, release ref policy, coverage, Go, dual-panel, fixed Linux FRP E2E, fault-injection, fuzz, security, container scan and release metadata gates; the revision-bound local collector remains 8 passed, 0 failed and 1 blocked because reviewed external Provider/target/signing/owner evidence is still absent.

- Local certificate-integrity regression: `RouterCertificates` now rejects a non-empty database `cert_hash` that does not match the loaded certificate file's SHA-256 before decrypting or installing the key pair; the service test covers matching, mismatch, and restoration while preserving the last-good caller contract. This is local evidence only until the next hosted verification and does not sign off real SNI deployment.
- Latest hosted verification: commit [`e7b7325`](https://github.com/sshiong/frp-panel-platform-v3/commit/e7b7325a34d82d9d177eb6804b644784e62c17bc) passed [`ci` run 31335409986](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31335409986) and [`CodeQL` run 31335409940](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31335409940). The run covered the official Cloudflare endpoint policy regression, Go coverage, dual-panel build/WCAG, fixed Linux FRP E2E, fault injection, fuzz, security, container scan and release metadata; external Provider/target-hardware/signing/owner evidence remains absent.
- Latest hosted verification: commit [`4a94c41`](https://github.com/sshiong/frp-panel-platform-v3/commit/4a94c4107fd46ffa439a9aba1018e252a2f000eb) passed [`ci` run 31334721807](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31334721807) and [`CodeQL` run 31334721817](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31334721817). The run covered the certificate hash regression, Go coverage, dual-panel build/WCAG, fixed Linux FRP E2E, fault injection, fuzz, security, container scan and release metadata; external Provider/target-hardware/signing/owner evidence remains absent.
- Latest local regression before this documentation refresh, bound to commit [`db5c64b`](https://github.com/sshiong/frp-panel-platform-v3/commit/db5c64bdd262a06074a6be055a424439cef3ea54), re-ran Server/Client race tests, performance profiles, both panels' authenticated WCAG/keyboard/mobile surfaces, and Secret scan successfully. The fixed-artifact `make external-acceptance` report recorded 8 passed, 0 failed and 1 blocked with exit code `2`; the only blocked step was the deliberately missing reviewed external Provider/ACME/target-environment/signing evidence.
- Latest evidence snapshot before this documentation-only update: commit [`48a478b`](https://github.com/sshiong/frp-panel-platform-v3/commit/48a478baf8837292348e6230b257d99395d7cf37) passed [`ci run 31337487666`](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31337487666) and [`CodeQL run 31337487642`](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31337487642). The corresponding `make external-acceptance` report records 9 passed, 0 failed and 1 blocked: local contract, migration, security, license, build, performance, fixed FRP TCP, FRPC verify and Plugin E2E passed; the only blocked step is the deliberately absent reviewed Provider/target/signing/owner evidence. The JSON report itself remains authoritative for the exact current revision binding.
- Latest local fixed-artifact collector, bound to code revision [`d9ec5ba`](https://github.com/sshiong/frp-panel-platform-v3/commit/d9ec5badbf60c06cbd57e7a8cdb1367b967bcc9c): the official FRP v0.68.0 Darwin ARM64 archive passed its GitHub release digest `9f344774971dfb9ae90ee1f633de68d1755c98510227a67289ec78081fc5c8fa`, and the extracted `frps`/`frpc` SHA-256 values were `c2d1397344a05b66b11af8e8b8c5cc295b198021d000f59adea50fa9e321414a` and `33b42679872d57790de7d610b17fc9783b6770a4813894365b44bc0d002af7d9`. Local contract, migration, security, license, build, performance, native TCP E2E, fixed FRPC `verify`, and real FRPS/FRPC Plugin network E2E all passed; the report records 9 passed, 0 failed and 1 blocked with mode `0600`. The only blocked step is the missing reviewed Cloudflare/ACME/TLS/target-environment/signing/owner evidence; no external pass is inferred.
- Latest runtime and visual verification for revision [`9dff7d1`](https://github.com/sshiong/frp-panel-platform-v3/commit/9dff7d1ba2cca5825c304019ef6d09c4a39dbce2): the built Server and Client binaries started independently, returned successful `/healthz` responses, served their embedded Admin/Client JavaScript entry assets, generated the Router `last-good` snapshot, and kept Client runtime secrets cleared without an active Session. `make accessibility` passed the entry, authenticated, keyboard, reduced-motion, WCAG 2.1 AA and 390px overflow checks; visual QA confirms the graphite/ivory/steel-blue/state-color system without pale purple.
- Latest hosted verification for revision [`9dff7d1`](https://github.com/sshiong/frp-panel-platform-v3/commit/9dff7d1ba2cca5825c304019ef6d09c4a39dbce2): [`ci run 31364166157`](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31364166157) and [`CodeQL run 31364166115`](https://github.com/sshiong/frp-panel-platform-v3/actions/runs/31364166115) passed, including coverage, Go race/static analysis, both panel builds and WCAG checks, fixed Linux FRP E2E, fault injection, fuzz, security, container scanning and release metadata. The revision-bound external collector remains `blocked` with 9 passed, 0 failed and 1 blocked because the GitHub `external-acceptance` environment has no configured secrets or reviewed Provider/target/signing/owner evidence; PR #2 remains open for required human review.

## Not signed off yet

Linux/production FRPS/FRPC Plugin compatibility matrix, Cloudflare Sandbox timeout compensation, ACME Staging against a real CA, Router TLS SNI certificate hot reload, a clean-host disaster restore drill, production deployment capacity/network sign-off, cosign signing, and a production HTTPS deployment remain release gates. The fixed 2 vCPU/2 GiB reference performance profile has passed; local tests and hosted CI never mark those external integrations active.
